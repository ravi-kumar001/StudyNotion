The node-cron module is tiny task scheduler in pure JavaScript for node.js based on GNU crontab. This module allows you to schedule task in node.js using full crontab syntax.

Errors below

in Auth controller sentOtp hf user again generate otp with same email if this is registered

read about autoPrefixer

create a function after userDelete cloudinary also delete its everything

create a validation during when a user signup then make sure user write a same email otherwise send response
create a validation during when a user login then make sure user write a same email otherwise send response

make if we course delete then delete video also with cloudinary